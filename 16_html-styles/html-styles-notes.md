<!-- +++++++++++++++ HTML Styles +++++++++++++++ -->
=> style attribute → used to add CSS styles directly to an HTML element.
=> Common CSS properties:
color → sets text color
background-color → sets element’s background color
font-family → sets font type
font-size → sets text size
text-align → sets horizontal text alignment (left, right and center)